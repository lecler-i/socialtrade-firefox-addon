{
  "scripts": {
    "test": "echo 'No tests yet :('",
    "dev": "jpm watchpost",
    "install:bower": "bower install && bower-installer",
    "build:firefox": "web-ext sign --api-key ${JPM_API_KEY} --api-secret ${JPM_API_SECRET} -s build/",
    "build:chrome": "web-ext build -s build/ && rename .zip .crx web-ext-artifacts/*.zip"
  },
  "devDependencies": {
    "bower": "^1.7.9",
    "bower-installer": "^1.2.0",
    "web-ext": "^1.5.0"
  },
  "dependencies": {}
}
